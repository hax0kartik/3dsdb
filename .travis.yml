language: python
sudo: false

python:
  - 3.6

# install dependencies
install:
- pip install -r requirements.txt

script:
- travis_wait 40 python 3dsdb.py # Yeah this script is fucking slow

after_success:
  - git config --global user.email "pokemonemerald0@gmail.com" 
  - git config --global user.name "Travis CI" 
  - git rm origin
  - git remote add origin https://hax0kartik:$GITHUB_API_KEY@github.com/hax0kartik/3dsdb.git
  - git add -A
  - git commit -m "New titles added [skip ci]"
  - git push origin master -fq > /dev/null
  - echo "Done."